## 1. Module Foundation
- [x] 1.1 Create `src/modules/home/components/LandingHero/` structure.
- [x] 1.2 Implement `index.ts` to export the main Hero container.

## 2. Component Implementation
- [x] 2.1 **FluxSlogan**: Implement character-based blur reveal and viscosity physical displacement using Framer Motion.
- [x] 2.2 **TrailGrid**: Implement static 12-col grid with a cursor-following light trail (Canvas or SVG).
- [x] 2.3 **CoordinateNav**: Implement the numeric-indexed navigation with morphing underlines.

## 3. Integration
- [x] 3.1 Mount components in `src/pages/index.astro`.
- [x] 3.2 Refine language redirect logic to only trigger when the user explicitly clicks a localized link or as a transparent background process.

## 4. Verification
- [x] 4.1 Run `pnpm check` and verify performance (LCP) on the new landing page.

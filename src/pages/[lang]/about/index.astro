---
import { AboutContent } from '../../../modules/about/components/AboutContent';
import intellectProgress from '../../../modules/infra/data/intellect-progress.json';
import BaseLayout from '../../../modules/infra/layouts/BaseLayout.astro';

export function getStaticPaths() {
  return [{ params: { lang: 'en' } }, { params: { lang: 'zh' } }];
}

const { lang } = Astro.params;
---

<BaseLayout title="About" lang={lang}>
  <div class="py-12">
    <AboutContent client:load lang={lang as 'en' | 'zh'} intellectProgress={intellectProgress} />
  </div>
</BaseLayout>
